{
  "version": 2,
  "installCommand": "pnpm install",
  "buildCommand": "tsc --build artifacts/api-server/tsconfig.json",
  "builds": [
    {
      "src": "artifacts/api-server/src/index.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "artifacts/api-server/src/index.ts"
    }
  ],
  "env": {
    "NODE_ENV": "production"
  }
}
